# merge-docs：合并文档

> 来源：`../routine/彩文：合并文档.txt`
> 触发：将工作目录中的分章节文档合并为完整文档。
> 工具：`merge_file`（见 `../tools.md`；输出 .md 自动转 Word/PDF）。

## 要求

1. 首先分析当前任务的执行情况：如果当前目录中已经有很多分章节文档，则使用 `merge_file` 对文档进行合并。
2. **如果文档章节缺失，请先补写缺失的文档，之后再合并。**
