# svg-diagram：绘制矢量示意图（SVG）

> 来源：`../routine/彩图：绘制矢量示意图（SVG）.txt`
> 触发：在文档中加入 SVG 矢量示意图。
> 通用规范：SKILL.md §2.1（SVG 绘制规范：大文本框、避免重叠、`fill='none'`、字体）。工具：`edit_file` / `convert_docs_to_markdown`（见 `../tools.md`）。

## 要求

1. 在文档中加入 SVG 图示，按照 markdown 中加入 SVG 图的格式书写，用于分析用户给出的数据或者解释用户提示词的内容。
2. **要求：至少在文档中绘制三张图。**
3. 请将用户的已有文档转为 markdown 格式，或生成新的 markdown 文件，在这个 markdown 文件中绘图。
4. 如果用户没有指定文档数量，则尽量写入多个文档，每个文档一个图。
